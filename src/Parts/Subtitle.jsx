import React from "react";

function Subtitle({ children }) {
  return (
    <div className="mb-3 mt-12 font-poppins text-lg font-semibold text-blue-700">
      {children}
    </div>
  );
}

export default Subtitle;
